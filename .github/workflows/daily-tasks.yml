name: Daily Tasks

on:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  daily-checks:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Check A
        continue-on-error: true
        run: |
          echo "Running check A"
          exit 0

      - name: Check B
        continue-on-error: true
        run: |
          echo "Running check B"
          exit 0

      - name: Check C
        continue-on-error: true
        run: |
          echo "Running check C"
          exit 0