name: Daily Tasks

on:
  workflow_dispatch:

jobs:
  basic-check:
    runs-on: ubuntu-22.04  # Explicitly use 22.04 instead of latest
    steps:
      - name: Basic Task
        run: |
          mkdir -p reports
          echo "Basic check completed successfully" > reports/result.txt
          cat reports/result.txt