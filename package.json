{
  "name": "claude-api-integrations",
  "version": "1.0.0",
  "description": "Claude API integrations and examples",
  "scripts": {
    "test": "node -e 'process.exit(0)'",
    "docs:generate": "node -e 'process.exit(0)'",
    "security-scan": "node -e 'process.exit(0)'",
    "perf-check": "node -e 'process.exit(0)'"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}