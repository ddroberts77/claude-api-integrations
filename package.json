{
  "name": "claude-api-integrations",
  "version": "1.0.0",
  "description": "Claude API integrations and examples",
  "scripts": {
    "test": "echo \"Info: no test specified\" && exit 0",
    "docs:generate": "echo \"Info: docs generation placeholder\" && exit 0",
    "security-scan": "echo \"Info: security scan placeholder\" && exit 0",
    "perf-check": "echo \"Info: performance check placeholder\" && exit 0"
  }
}